<%= form_with(model: listing_photo, local: true) do |form| %>
  <% if listing_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(listing_photo.errors.count, "error") %> prohibited this listing_photo from being saved:</h2>

      <ul>
      <% listing_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
