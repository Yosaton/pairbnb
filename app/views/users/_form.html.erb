<%= form_for :user, url: update_user_path, method: :patch do |f| %>

	<div>
		<%= f.label :email, "New Email Address" %>
		<%= f.text_field :email %>

		<%= f.label :avatar_image, "Upload a profile avatar"%>
		<%= f.file_field :avatar_image %>
	</div>

	<br /><%= f.submit "Save Changes"%>
<% end %>